"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "var(--font-switzer), 'Helvetica Neue', Arial, sans-serif",
  },
  // you can also customize palette, components, etc.
});

export default theme;
