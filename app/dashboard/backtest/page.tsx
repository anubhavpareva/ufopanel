import PageHeader from "@/Components/Shared-ui/PageHeader";
import { Box } from "@mui/material";

export default function BackTest() {
  return (
    <Box width={"100%"}>
      <PageHeader title="Backtest" />
      <Box px={1}>BackText</Box>
    </Box>
  );
}
